# nixpacks.toml

[phases.setup]
aptPkgs = ['libcairo2-dev', 'pkg-config']

# Optional but recommended – sometimes helps with Python extension builds
aptPkgs = ['libcairo2-dev', 'pkg-config', 'python3-dev', 'build-essential']
