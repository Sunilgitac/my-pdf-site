[phases.setup]
nixPkgs = ["python311", "libreoffice-bin"]

[phases.build]
# --no-cache-dir prevents using corrupted build data from previous crashes
cmds = ["python -m pip install --no-cache-dir -r requirements.txt"]
